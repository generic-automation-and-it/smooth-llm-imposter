# Examples & Evals — CodexToOpenAiSdk Transformer

The only place in this HLD where code/payloads may appear (per the create-hld skill). These are
empirical characterizations of the strict upstream, used to derive the (future) normalizer's
request-edit rules from observed behavior rather than assumption.

- [`upstream-tool-validation.md`](./upstream-tool-validation.md) — what `opencode → Moonshot
  kimi-k2.7-code` accepts/rejects in the `tools` array, with reproducible request bodies and
  observed results, plus the upstream contract and normalization implications.
